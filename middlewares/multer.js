//importo multer
//const multer = require('multer');

//definisco la cartella storage in cui effettuare l'upload
//const storage = multer.diskStorage({
//definisco la cartella di destinazione
//destination: ":/public/movies_img",
//definisco il nome del file
//filename: (req, file, cb) => {
//in questa funzione definiamo una variabile che conterrà il nome della foto in generale
//console.log(file);
//const uniqueName = `${Date.now()}-${file.originalname}`;
//cb(null, uniqueName);
//}
//});

//const upload = multer({ storage });

//module.exports = upload